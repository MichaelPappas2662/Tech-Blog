<div class="card mb-5">
    <div class="card-header bg-secondary">
        <h4 class="card-title"><a class="text-info" href="/post/{{id}}">{{title}}</a></h4>
        <h6 class="card-subtitle mb-2 text-light">Posted by {{user.username}} on {{created_at}}</h6>
    </div>
    <div class="card-body">
        <p class="card-text">{{content}}</p>
    </div>
</div>